<?xml version="1.0"?>
<odoo>
    <record id="domaines_proprietes_action" model="ir.actions.act_window">
        <field name="name">Propriétes</field>
        <field name="res_model">domaines_proprietes</field>
        <field name="view_mode">tree,form,kanban</field>
        <field name="context">{'search_default_proprietes_disponibles': True}</field>
    </record>

    <record id="domaines_proprietes_types_action" model="ir.actions.act_window">
        <field name="name">Property Types</field>
        <field name="res_model">domaines_proprietes_types</field>
        <field name="view_mode">tree,form</field>
    </record>

    <record id="domaines_proprietes_tags_action" model="ir.actions.act_window">
        <field name="name">Property Tags</field>
        <field name="res_model">domaines_proprietes_tags</field>
        <field name="view_mode">tree,form</field>
    </record>

    <record id="domaines_proprietes_offers_action" model="ir.actions.act_window">
        <field name="name">Property Offers</field>
        <field name="res_model">domaines_proprietes_offer</field>
        <field name="view_mode">tree</field>
        <field name="domain">[('property_type_id', '=', active_id)]</field>
    </record>

</odoo>